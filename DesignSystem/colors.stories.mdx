import { Meta, ColorPalette, ColorItem } from '@storybook/blocks';

<Meta title="Design System/Colors" />

## Colors

### DIVIDER

<ColorPalette>
  {Object.entries(
    getColors('divider').map(([key, color]) => (
      <ColorItem
        key={key}
        title={key}
        color={[color]}
      />;
    ))
  )}
</ColorPalette>
